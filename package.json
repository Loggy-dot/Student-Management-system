{
  "name": "students-management-system",
  "version": "1.0.0",
  "description": "Student Management System with frontend and backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm run setup",
    "setup": "cd backend && npm install && cd ../student-management-frontend && npm install --include=dev",
    "build": "cd student-management-frontend && npm install --include=dev && npm run build",
    "dev": "cd backend && npm run dev",
    "test": "cd backend && npm test"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "axios": "^1.12.1",
    "express": "^5.1.0"
  },
  "keywords": [
    "student",
    "management",
    "system",
    "education"
  ],
  "author": "",
  "license": "ISC"
}
